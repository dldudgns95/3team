<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.withmall.dao.AdminMapper">



   <insert id="insertPrdt" parameterType="ProductDto">

      INSERT INTO PRODUCT (
          PRDT_NUM,
          PRDT_NAME,
          PRDT_TITLE,
          PRDT_INFO,
          PRDT_REAL_PRICE,
          PRDT_STOCK,
          CATEGORY_NUM
      ) VALUES (
          #{prdtNum},
          #{prdtName},
          #{prdtTitle},
          #{prdtInfo},
          #{prdtRealPrice},
          #{prdtStock},
          #{categoryNum}
      );
  </insert>


  

  
  <insert id="insertPrdtImage" parameterType="PrdtImageDto">
    INSERT INTO PRODUCT_IMAGE(
        PRDT_NUM
      , IMAGE_PATH
      , FILESYSTEM_NAME
    ) VALUES(
        #{prdtNum}
      , #{imagePath}
      , #{filesystemName}
    )    
  </insert>

  

  
  
</mapper>
